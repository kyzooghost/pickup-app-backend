import { resetStudentTable } from '../src/functions';

describe('Internal functions', async () => {
  describe('#resetStudentTable()', async () => {
    it('', async () => {
      const data = await resetStudentTable();
      console.log(data);
      return;
    }).timeout(10000);
  });
});
